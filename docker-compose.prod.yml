services:
  site:
    build:
      context: .
      dockerfile: Dockerfile.prod
    image: jhuang2023-site:latest
    container_name: jhuang2023-cv-app
    ports:
      - "127.0.0.1:${JHUANG2023_PORT:-8200}:80"
    restart: unless-stopped
